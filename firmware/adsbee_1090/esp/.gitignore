**.pem
# ESP IDF stores a bunch of stuff in settings that isn't applicable across developers or machines.
.vscode/settings.json
# Generated by debug builds (layered from sdkconfig + sdkconfig.debug); delete to force regeneration.
sdkconfig_debug
# Boot-splash image data, generated from images/adsbee_logo.png by firmware/scripts/gen_splash_image.py
# on every ESP build (see build.sh generate_splash / firmware/README.md).
main/peripherals/display/splash_data.hh
main/peripherals/display/splash_data.cpp